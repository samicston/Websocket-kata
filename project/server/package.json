{
    "name": "websocketbackend",
    "version": "1.0.0",
    "scripts": {
        "start": "pm2 kill && pm2 start --update-env --no-daemon process.config.js",
        "kill": "pm2 kill",
        "test": "ava test/**/*spec.js --verbose"
    },
    "private": true,
    "dependencies": {
        "express": "^4.16.4",
        "pm2": "^3.2.2",
        "socket.io": "^2.1.1"
    }
}
